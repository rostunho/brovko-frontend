export const getAllProducts = ({ products }) => products.items;

export const selectTotalPages = ({ products }) => products.totalPages;

export const getProductsByCategory = ({ products }) => products.items;

export const getProductsByKeywords = ({ products }) => products.items;
